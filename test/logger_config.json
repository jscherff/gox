
type Logger struct {
	*MLogger
	LogFile string
	LogFlags int
	Stdout bool
	Stderr bool
}

type Loggers map[string]*Logger

{
	"Loggers": {
		"access": {
			"LogFile": "system.log",
			"LogFlags": ["date","time","shortfile"],
			"Stdout": false,
			"Stderr": false
		}
	}
}
